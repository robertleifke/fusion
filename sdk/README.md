# Fusion SDK

TypeScript/Rust client code for:
- instruction builders
- account decoding helpers
- integration utilities for apps and market makers

The SDK should be generated from `idl/fusion.json` and then extended with
ergonomic helpers.
